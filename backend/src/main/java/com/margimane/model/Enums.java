package com.margimane.model;

public class Enums {
    public enum Role { USER, ADMIN }
    public enum MenuCategory { TEA, DOSA, BATH, MEALS }
    public enum PaymentStatus { PENDING, APPROVED, REJECTED }
    public enum PointsTransactionType { EARN, REDEEM, ADJUSTMENT }
}
